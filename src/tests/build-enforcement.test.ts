// Temporary test to verify build enforcement works
describe('Build enforcement test', () => {
  it('should fail to test build enforcement', () => {
    expect(true).toBe(false); // This will fail
  });
});